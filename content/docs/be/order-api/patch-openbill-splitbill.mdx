---
title: PATCH Open Bill > Split Bill > Count Order
description: "Update total order Open bill >  Split Bill"
---

# Endpoint

`PATCH/split-bill/count/:idOrder`

```ts
https://api.tantri.id/split-bill/count/:idOrder
```

## 🔑 Autentikasi

Endpoint ini memerlukan autentikasi menggunakan token JWT. Jika pengguna belum login, maka pengguna akan diarahkan ke halaman login.

```http
Authorization: Bearer <your_token>
```

# Deskripsi

Endpoint ini digunakan untuk update total order Open bill berbarengan dengan split bill.

## Request Body

```json
{
    "totalSplitBill": number
}
```
